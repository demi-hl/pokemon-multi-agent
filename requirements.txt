# Pokemon Multi-Agent System Dependencies
# Poke Agent - AI-Powered Pokemon TCG Dashboard

# Core server
flask>=3.0.0
flask-cors>=4.0.0
flask-compress>=1.14
requests>=2.31.0
gunicorn>=21.0.0

# Discord bot (multi-user support)
discord.py>=2.3.0
aiohttp>=3.9.0

# Environment variables
python-dotenv>=1.0.0

# Security (encrypted payment storage)
cryptography>=41.0.0

# Secure Payment Processing (cards never touch our servers)
stripe>=7.0.0

# Card dashboard API (sets, search, collection, alerts, grading)
httpx>=0.24.0

# Web scraping (for stock checking)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Signal system (event-driven stock checking)
blinker>=1.7.0

# Optional packages (comment out if deploy fails):
# undetected-chromedriver>=3.5.0  # Needs Chrome installed
# selenium>=4.15.0                 # Needs Chrome installed
